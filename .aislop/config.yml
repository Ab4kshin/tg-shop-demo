# Конфиг aislop.
# Исключаем виртуальное окружение Python и артефакты сборки — это чужой код
# (библиотеки), а не наш. node_modules/dist/build aislop исключает сам.
exclude:
  - "**/.venv/**"
  - "**/venv/**"
  - "**/__pycache__/**"
  - "**/node_modules/**"
  - "**/dist/**"
  - "**/build/**"
